{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: DeviceNet Checker (Debug)",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/dnet_checker.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "justMyCode": false,
            "qt": "pyside6",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}
